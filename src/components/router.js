import { fromClass } from 'karet';
import * as RR from 'react-router-dom';

export const Route = fromClass(RR.Route);
export const Switch = fromClass(RR.Switch);
export const Link = fromClass(RR.Link);
export const NavLink = fromClass(RR.NavLink);
